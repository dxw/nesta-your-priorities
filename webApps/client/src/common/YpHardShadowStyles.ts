import { css } from "lit";

export const YpHardShadowStyles = css`
  .yp-hard-shadow-box {
    border: 2px solid var(--yp-hard-shadow-border-color, #14151a);
    box-shadow: 6px 6px 0 0 var(--yp-hard-shadow-color, #e0218a);
  }
`;
